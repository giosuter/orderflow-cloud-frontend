# OrderFlow Cloud – Frontend (Angular)

This is the Angular frontend for the **OrderFlow API**.

- Backend (dev): Spring Boot under context path `/orderflow-api`
- Frontend (dev): Angular 19, standalone components
- API contract: `/api` under `/orderflow-api`

---

## 1. Local development

### 1.1 Prerequisites

- Node.js (v23.x as on your machine)
- Angular CLI (global):

```bash
npm install -g @angular/cli