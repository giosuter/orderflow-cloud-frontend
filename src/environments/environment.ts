export const environment = {
  production: false,

  // With proxy.conf.json, keep it empty so HttpClient calls go to Angular dev server
  // and are proxied to http://localhost:8080.
  apiBaseUrl: '',
};